"""Bounded complementary composition; historical studies remain immutable."""

# Install before importing source readers so all captured aliases share one
# invocation-local, stat-aware memo. This does not change any scientific code.
from scripts.evaluation.verification_cache.file_hash_cache import install

install()
